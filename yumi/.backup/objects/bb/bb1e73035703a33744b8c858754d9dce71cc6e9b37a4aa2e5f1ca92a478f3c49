# Yumi 项目结构地图

## 📁 项目概览

**Yumi** 是一个 Android CPU 调度控制系统，由 Rust 守护进程、eBPF 内核探针和 WebUI 管理界面组成。

**项目版本**: v2.0.1  
**许可证**: GPL-3.0  
**作者**: yuki (loyeturz@163.com)  
**仓库**: https://github.com/imacte/yumi

---

## 🗂️ 根目录文件

| 文件 | 作用 |
|------|------|
| `README.md` / `README.en.md` | 项目文档，包含功能介绍、安装说明、配置指南 |
| `Cargo.toml` / `Cargo.lock` | Rust 工作空间配置，定义项目依赖和构建配置 |
| `LICENSE` | GPL-3.0 开源许可证 |
| `.gitignore` | Git 忽略规则，排除构建产物、IDE 文件等 |
| `build.rs` | 构建脚本，自动编译 eBPF 内核程序 |

---

## 🔧 开发工具与配置

| 文件/目录 | 作用 |
|-----------|------|
| `.backup/` | 备份目录，包含聊天记录和对象数据（可能是开发过程中的数据备份） |
| `.cargo/` | Cargo 配置目录 |
| `.cargo/config.toml` | Cargo 别名配置，定义 `xtask` 命令运行 xtask 包 |

---

## 📦 核心源码目录

### `src/` - Rust 守护进程源码

| 文件/目录 | 作用 |
|-----------|------|
| `main.rs` | 程序入口，初始化环境、加载配置、启动监控和调度线程 |
| `common.rs` | 公共类型定义，包含 `DaemonEvent` 事件枚举（ModeChange、FrameUpdate、SystemLoadUpdate 等） |
| `fas_types.rs` | FAS 帧感知调度相关类型定义 |
| `i18n.rs` | 国际化支持，基于 Fluent 的多语言系统 |
| `logger.rs` | 日志系统初始化，支持多级别日志输出 |
| `utils.rs` | 工具函数集合 |

### `src/monitor/` - 监控模块

| 文件 | 作用 |
|------|------|
| `mod.rs` | 监控模块入口，协调各监控组件 |
| `app_detect.rs` | 前台应用检测，通过 cgroup 读取 top-app 进程列表 |
| `cpu_monitor.rs` | CPU 利用率监控，通过 eBPF sched_switch tracepoint 采集 |
| `fps_monitor.rs` | 帧率监控，通过 eBPF queueBuffer uprobe 捕获渲染帧 |
| `screen_detect.rs` | 屏幕状态检测，通过 Netlink uevent 监听亮灭事件 |
| `config.rs` | 监控配置相关类型定义 |

### `src/scheduler/` - 调度模块

| 文件/目录 | 作用 |
|-----------|------|
| `mod.rs` | 调度模块入口，处理事件总线、管理调度策略 |
| `config.rs` | 配置文件解析，支持 YAML 热重载 |
| `scheduler.rs` | 调度器核心逻辑 |
| `cpu_load_governor.rs` | CPU 负载调速器 (CLG)，基于实时负载的自适应调频 |
| `fas/` | FAS 帧感知调度子模块 |

### `src/scheduler/fas/` - FAS 帧感知调度

| 文件 | 作用 |
|------|------|
| `mod.rs` | FAS 模块入口 |
| `controller.rs` | FAS 控制器，管理帧调度状态和策略 |
| `pid.rs` | PID 控制器实现，用于帧时间-频率映射 |
| `pid_jank.rs` | Jank 卡顿检测和冷却机制 |
| `frame_pipeline.rs` | 帧处理流水线，分析帧间隔和性能指标 |
| `gear_state.rs` | 帧率档位状态管理，自动升降档逻辑 |
| `fps_window.rs` | FPS 滑动窗口，计算平均帧率 |
| `policy_controller.rs` | 策略控制器，管理 FAS 调度策略 |
| `policy_mgmt.rs` | 策略管理，处理配置更新和策略切换 |

---

## 🔧 eBPF 内核探针

### `yumi-ebpf/` - eBPF 程序

| 文件 | 作用 |
|------|------|
| `Cargo.toml` | eBPF 程序依赖配置，使用 aya-ebpf 框架 |
| `src/main.rs` | eBPF 内核程序，包含两个探针：<br>1. CPU 探针：挂载 sched_switch tracepoint，采集 CPU 利用率<br>2. FPS 探针：挂载 queueBuffer uprobe，捕获渲染帧 |

---

## 📱 Magisk/KernelSU 模块

### `module/` - 模块文件

| 文件/目录 | 作用 |
|-----------|------|
| `module.prop` | 模块元数据，包含版本、作者等信息 |
| `service.sh` | 模块启动脚本，系统启动后自动运行守护进程 |
| `customize.sh` | 模块安装脚本，检测系统环境并显示欢迎信息 |
| `uninstall.sh` | 模块卸载脚本，清理进程和配置 |
| `rules.yaml` | 调度规则配置，定义应用性能模式和 FAS 参数 |
| `config/` | 配置文件目录 |
| `config/config.yaml` | 主配置文件，定义性能模式、I/O 优化等参数 |
| `config/i18n/` | 国际化资源文件 |
| `scripts/` | 辅助脚本目录 |
| `scripts/disable_boost.sh` | 禁用系统 Boost 脚本，关闭内核级 Boost、温控降频、厂商调度优化等 |
| `META-INF/` | 模块元数据目录，包含安装脚本签名等 |

---

## 🌐 WebUI 管理界面

### `webui/` - Vue.js WebUI

| 文件/目录 | 作用 |
|-----------|------|
| `package.json` | 前端项目配置，使用 Vue 3 + Vite + Vant UI |
| `vite.config.ts` | Vite 构建配置 |
| `tsconfig.json` | TypeScript 配置 |
| `index.html` | 入口 HTML 文件 |
| `src/` | Vue 源码目录 |
| `src/main.ts` | Vue 应用入口 |
| `src/App.vue` | 根组件 |
| `src/views/` | 页面视图组件 |
| `src/views/HomeView.vue` | 首页视图，显示状态概览和模式切换 |
| `src/views/ConfigEditorView.vue` | 配置编辑器，支持在线编辑 YAML 配置文件 |
| `src/views/AppRulesView.vue` | 应用规则管理，配置不同应用的性能策略 |
| `src/views/LogViewerView.vue` | 日志查看器，实时显示守护进程日志 |
| `src/stores/` | Pinia 状态管理 |
| `src/stores/scheduler.ts` | 调度器状态管理，处理配置和状态更新 |
| `src/stores/counter.ts` | 计数器状态（示例） |
| `src/router/` | Vue Router 路由配置 |
| `src/i18n/` | 前端国际化配置 |
| `src/kernelsu/` | KernelSU 集成相关 |
| `src/utils/` | 前端工具函数 |
| `public/` | 静态资源目录 |

---

## 🛠️ 构建工具

### `xtask/` - 构建任务工具

| 文件 | 作用 |
|------|------|
| `Cargo.toml` | 构建工具依赖配置 |
| `src/main.rs` | 构建任务主程序，自动化打包、发布等任务 |
| `src/zip_ext.rs` | ZIP 文件处理扩展 |

---

## 📋 更新信息

### `updateInformation/` - 更新元数据

| 文件 | 作用 |
|------|------|
| `update.json` | 更新元数据，包含版本号、下载链接、更新日志 URL |
| `changelog.md` | 更新日志，记录新功能、优化和修复 |

---

## ⚙️ GitHub 配置

### `.github/` - GitHub 配置

| 文件/目录 | 作用 |
|-----------|------|
| `dependabot.yml` | Dependabot 配置，自动更新依赖 |
| `workflows/` | GitHub Actions 工作流目录，用于 CI/CD |

---

## 🔄 架构流程

```
┌─────────────────────────────────────────────────────────────┐
│                      用户空间 (Rust)                          │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ app_detect   │  │ fps_monitor  │  │ cpu_monitor  │       │
│  │ (Cgroup)     │  │ (eBPF uprobe)│  │(eBPF trace) │       │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                 │                 │                │
│         │     ┌───────────┴─────────────────┘                │
│         │     │   DaemonEvent (mpsc channel)                 │
└─────────┼─────┼──────────────────────────────────────────────┘
          │     │
          ▼     ▼
┌─────────────────────────────────────────────────────────────┐
│                    Scheduler IPC 线程                         │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐                          │
│  │ FAS 控制器    │  │ CLG 负载调速 │                          │
│  │ (游戏场景)    │  │ (日常场景)    │                          │
│  └──────────────┘  └──────────────┘                          │
│               ▼                                              │
│     sysfs 频率写入 (FastWriter)                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      内核空间 (eBPF)                          │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐                          │
│  │ sched_switch │  │ queueBuffer  │                          │
│  │ tracepoint   │  │ uprobe       │                          │
│  └──────────────┘  └──────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 技术栈

| 组件 | 技术栈 |
|------|--------|
| **守护进程** | Rust 2024, Aya (eBPF), Tokio (异步), Serde (序列化) |
| **eBPF 探针** | Rust (aya-ebpf), BPF 内核程序 |
| **WebUI** | Vue 3, TypeScript, Vite, Vant UI, Pinia |
| **构建工具** | Cargo (Rust), xtask (自定义构建任务) |
| **模块系统** | Magisk/KernelSU 模块, Shell 脚本 |
| **配置格式** | YAML (配置文件), Fluent (国际化) |

---

## 🎯 主要功能模块

1. **eBPF 内核监控** - 零开销采集 CPU 利用率和帧率数据
2. **FAS 帧感知调度** - PID 控制器驱动的逐帧动态调频
3. **CLG 负载调速器** - 基于实时负载的自适应调频
4. **智能模式切换** - 根据前台应用自动调整性能模式
5. **温度感知调控** - 超温自动降频保护
6. **息屏节电模式** - 息屏自动进入 Doze 模式
7. **WebUI 管理** - 浏览器界面配置和监控
8. **多语言支持** - 中英文国际化

---

*文档生成时间：2026年7月20日*